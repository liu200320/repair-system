# v1.4 更新部署教程

> **适用场景**：已按《LINUX部署教程.md》完成初次部署，项目正在运行中，现在需要更新上线 v1.4 版本。  
> **操作原则**：拉取代码 → 补充配置 → 重建前端 → 重启后端，现有数据库数据完全不受影响。  
> **服务器系统**：Ubuntu 20.04 / 22.04 LTS

---

## 本次更新内容

| 类型 | 内容 |
|------|------|
| 新模块1 | 网络基础设施日常巡检（CRUD + 照片上传 + 单条/时间段批量 Word 导出） |
| 新模块2 | 门禁日常巡检（CRUD + 照片上传 + 单条/时间段批量 Word 导出） |
| 安全加固 | JWT 密钥强制通过环境变量设置、CORS 来源可配置、JWT 主动吊销（登出接口） |
| 数据库 | 自动补充 `users.token_version` 列；自动创建6张新表（网络/门禁巡检记录+照片表，以及预置地点表） |
| 导航优化 | 移除顶部「新建记录」快捷按钮，各模块自行新建；新增「网络巡检」「门禁巡检」导航项 |
| 导出清理 | 导出 Word 文件超过7天自动删除 |
| 前端依赖 | 锁定精确版本号 |

> ✅ **Nginx 配置无需修改**，原有维修记录、耗材管理数据完全不受影响。

---

## 第一步：拉取最新代码

登录服务器：

```bash
ssh root@你的服务器IP
cd /opt/repair-system
git pull
```

验证关键新文件已到位：

```bash
ls backend/app/models/network_inspection.py
ls backend/app/models/access_inspection.py
ls backend/app/api/v1/network_inspection.py
ls backend/app/api/v1/access_inspection.py
ls frontend/src/views/NetworkInspectionList.vue
ls frontend/src/views/AccessInspectionList.vue
# 每条命令显示文件路径即为成功
```

---

## 第二步：补充后端配置文件（**必须执行**）

v1.4 新增了 `SECRET_KEY` 等必填环境变量，旧 `.env` 里没有，需要手动追加。

### 2.1 生成随机密钥

```bash
source /opt/repair-system/backend/venv/bin/activate
python3 -c "import secrets; print(secrets.token_hex(32))"
deactivate
```

复制输出的那串64位随机字符串（如 `a3f8...`）。

### 2.2 追加配置

```bash
# 追加 SECRET_KEY（将 你的随机密钥 替换为上面复制的内容）
echo "SECRET_KEY=你的随机密钥" >> /opt/repair-system/backend/.env

# 追加 CORS 来源（将 IP 替换为你的服务器实际 IP 或域名）
echo "ALLOWED_ORIGINS=http://你的服务器IP" >> /opt/repair-system/backend/.env

# 追加导出文件保留天数（默认7天，可按需修改）
echo "EXPORT_RETENTION_DAYS=7" >> /opt/repair-system/backend/.env

# 追加 Token 过期时间（默认8小时，保持不变即可）
echo "ACCESS_TOKEN_EXPIRE_MINUTES=480" >> /opt/repair-system/backend/.env
```

### 2.3 验证配置内容

```bash
cat /opt/repair-system/backend/.env
```

确认输出包含以下字段（`SECRET_KEY` 的值应为你生成的随机字符串，不能是空值）：

```
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=Repair@2026
DB_NAME=repair_db
UPLOAD_DIR=uploads
MAX_FILE_SIZE=10485760
SECRET_KEY=a3f8...（你生成的随机字符串）
ALLOWED_ORIGINS=http://你的服务器IP
EXPORT_RETENTION_DAYS=7
ACCESS_TOKEN_EXPIRE_MINUTES=480
```

---

## 第三步：重新构建前端

前端源码有更新，需要重新编译：

```bash
cd /opt/repair-system/frontend
npm install
npm run build
```

> ⏳ 构建约需 1 分钟

构建完成后，部署到 Nginx 目录：

```bash
cp -r dist/* /var/www/repair-system/
chown -R www-data:www-data /var/www/repair-system/
```

验证新页面文件存在：

```bash
ls /var/www/repair-system/assets/ | grep -i network
# 应看到 NetworkInspection 相关的 .js 文件
```

---

## 第四步：重启后端（自动完成数据库迁移）

```bash
pm2 restart repair-api
```

等待约 5 秒后验证：

```bash
# 后端健康检查
curl http://127.0.0.1:8000/health
# 期望：{"status":"ok","version":"2.0.0"}

# 新接口是否可访问（返回 401 = 接口存在、认证正常）
curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/api/v1/network-inspections
# 期望：401
```

> 📌 后端启动时会**自动执行**以下操作，无需手动建表或导入数据：
> - 给 `users` 表补充 `token_version` 列（旧数据默认值0，不影响已有账号）
> - 创建 `network_locations` 表并写入21个网络巡检预置地点
> - 创建 `access_locations` 表并写入3个门禁预置地点（学校大门口 / 冻精站大门口 / 种畜推广中心大门口）
> - 创建 `network_inspection_records`、`network_inspection_photos` 表
> - 创建 `access_inspection_records`、`access_inspection_photos` 表

---

## 第五步：验证数据库新表

```bash
mysql -u root -p'Repair@2026' repair_db -e "SHOW TABLES;"
```

应看到新增了：

```
access_inspection_photos
access_inspection_records
access_locations
network_inspection_photos
network_inspection_records
network_locations
```

以及 `users` 表已自动补充 `token_version` 列：

```bash
mysql -u root -p'Repair@2026' repair_db -e "SHOW COLUMNS FROM users LIKE 'token_version';"
# 应显示一行 token_version 字段
```

---

## 第六步：重载 Nginx（让前端更新生效）

```bash
systemctl reload nginx

# 验证 Nginx 无报错
nginx -t
# 期望：configuration file ... syntax is ok
```

---

## 第七步：测试新功能

打开浏览器，访问 `http://你的服务器IP`，使用管理员账号登录。

**测试清单：**

- [ ] 左侧侧边栏出现「网络巡检」「门禁巡检」菜单项
- [ ] 全局「新建记录」按钮已移除（各模块自行新建）
- [ ] **网络巡检**：列表页正常、新建记录地点下拉可选21个预置地点、照片上传、单条/批量导出 Word
- [ ] **门禁巡检**：列表页正常、新建记录地点下拉可选3个门禁地点、照片上传、单条/批量导出 Word
- [ ] **点位管理**：进入后显示三个标签（维修点位 / 网络巡检点位 / 门禁点位），三个标签均可搜索、新增、删除
- [ ] 原有「维修记录」「耗材管理」功能正常使用（确认未受影响）

---

## 快速操作汇总

熟悉流程后，完整更新命令（服务器端）：

```bash
cd /opt/repair-system

# 1. 拉取代码
git pull

# 2. 重建前端并部署
cd frontend && npm install && npm run build
cp -r dist/* /var/www/repair-system/ && chown -R www-data:www-data /var/www/repair-system/
cd ..

# 3. 重启后端（自动建表迁移）
pm2 restart repair-api

# 4. 重载 Nginx
systemctl reload nginx

# 5. 验证
curl http://127.0.0.1:8000/health
```

---

## 回滚方案

如果更新后出现异常：

```bash
cd /opt/repair-system

# 查看提交历史，找到更新前的版本 hash
git log --oneline -5

# 回滚代码（将 abc1234 替换为目标 hash）
git checkout abc1234

# 重建前端
cd frontend && npm run build
cp -r dist/* /var/www/repair-system/
cd ..

# 重启后端
pm2 restart repair-api

# 重载 Nginx
systemctl reload nginx
```

> ✅ 回滚只影响代码，已创建的巡检记录数据保留在数据库，不会丢失。

---

## 常见问题

### Q1：重启后 `pm2 list` 显示 `errored`

```bash
pm2 logs repair-api --lines 50
```

| 错误信息 | 解决方法 |
|----------|----------|
| `RuntimeError: 环境变量 SECRET_KEY 未设置` | 执行第二步，在 `.env` 中追加 `SECRET_KEY=随机密钥` |
| `ModuleNotFoundError: No module named 'app.models.network_inspection'` | `git pull` 确认新文件已拉取 |
| `Can't connect to MySQL` | `systemctl start mysql` |

### Q2：浏览器无「网络巡检」菜单

强制刷新：`Ctrl + Shift + R`（Windows）或 `Cmd + Shift + R`（Mac）。

仍无变化则确认前端文件已更新：

```bash
ls /var/www/repair-system/assets/ | grep -i network
```

### Q3：巡检地点下拉为空

后端启动时会自动检查并写入种子数据：
- 网络巡检地点：21个
- 门禁地点：3个（学校大门口 / 冻精站大门口 / 种畜推广中心大门口）

如果下拉仍为空，重启一次后端：

```bash
pm2 restart repair-api
```

### Q4：上传照片失败（413 错误）

```bash
grep client_max_body_size /etc/nginx/sites-available/repair-system
# 应显示 20M 或以上
```

如果没有该配置，在 Nginx 配置的 `server {}` 块中加入：

```nginx
client_max_body_size 20M;
```

然后 `systemctl reload nginx`。

---

## 常用维护命令速查

| 操作 | 命令 |
|------|------|
| 查看后端状态 | `pm2 list` |
| 查看后端实时日志 | `pm2 logs repair-api` |
| 重启后端 | `pm2 restart repair-api` |
| 重载 Nginx（不中断） | `systemctl reload nginx` |
| 重启 MySQL | `systemctl restart mysql` |
| 查看 Nginx 错误日志 | `tail -50 /var/log/nginx/error.log` |
| 查看后端最近50行日志 | `pm2 logs repair-api --lines 50` |

---

*文档版本：v1.4（网络巡检模块更新版） | 更新日期：2026-07-21*
