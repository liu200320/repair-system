# v1.5 安全加固更新部署教程

> **适用场景**：已按 v1.4 完成部署，项目正在运行中，现在需要更新上线 v1.5 安全加固版本。  
> **操作原则**：拉取代码 → 安装新依赖 → 补充配置 → 重建前端 → 重启后端，现有数据库数据完全不受影响。  
> **服务器系统**：Ubuntu 20.04 / 22.04 LTS

---

## 本次更新内容（安全加固）

| 类型 | 内容 |
|------|------|
| 登录防暴力破解 | 新增 slowapi 速率限制，同一 IP 每分钟最多尝试登录10次，超出返回 429 |
| 管理员初始密码 | 硬编码的 `admin123` 改为必须从 `ADMIN_INIT_PASSWORD` 环境变量读取（**仅首次建库时生效，现有账号不受影响**） |
| 文件上传 DoS 防护 | 改为分块读取文件，超出大小限制立即中断，避免超大文件耗尽服务器内存 |
| 导出路径防注入 | 批量导出接口对日期参数增加严格格式校验（必须为 `YYYY-MM-DD`），防止路径遍历 |
| 密码复杂度提升 | 新建/修改密码要求至少8位，且必须同时包含字母和数字（存量密码不受影响，下次修改时生效） |
| 健康检查 | `/health` 响应不再返回版本号，避免版本信息暴露 |
| 配置安全 | 根目录 `.env` 加入 `.gitignore`；docker-compose 移除弱回退密码 `repair123` |

> ✅ **本次更新无任何数据库结构变更**，无需建表或迁移，现有维修记录、巡检记录、耗材数据、用户账号完全不受影响。  
> ✅ **Nginx 配置无需修改。**

---

## 第一步：拉取最新代码

```bash
ssh root@你的服务器IP
cd /opt/repair-system
git pull
```

验证新文件已到位：

```bash
ls backend/app/core/limiter.py
# 显示路径即为成功
```

---

## 第二步：安装新依赖（slowapi 速率限制库）

本次新增了 `slowapi` 依赖，必须重新安装：

```bash
source /opt/repair-system/backend/venv/bin/activate
pip install -r backend/requirements.txt
deactivate
```

> 如果网络慢，可加清华镜像：`pip install -r backend/requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple`

验证安装成功：

```bash
source /opt/repair-system/backend/venv/bin/activate
python3 -c "import slowapi; print('slowapi OK')"
deactivate
# 期望输出：slowapi OK
```

---

## 第三步：检查配置文件

> **已有 `.env` 的用户**：原有配置继续有效，本步骤只需确认无需额外操作。

### 3.1 当前 .env 内容确认

```bash
cat /opt/repair-system/backend/.env
```

只要包含以下字段，**无需任何修改**即可继续：

```
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=Repair@2026
DB_NAME=repair_db
UPLOAD_DIR=uploads
SECRET_KEY=（你之前设置的随机密钥）
ALLOWED_ORIGINS=http://你的服务器IP
```

### 3.2 关于 ADMIN_INIT_PASSWORD（已有用户无需设置）

`ADMIN_INIT_PASSWORD` 仅在**数据库中没有任何用户时**（即全新建库）才会被读取，用于创建初始管理员。

- **你的系统已有运行记录和用户账号** → 此变量不会被触发，**不需要设置**。
- 如果你是在一台全新服务器上**第一次**部署，则需要添加：

```bash
# 仅全新部署时需要执行此命令
python3 -c "import secrets; print(secrets.token_urlsafe(16))"
# 将输出值填入：
echo "ADMIN_INIT_PASSWORD=上面输出的值" >> /opt/repair-system/backend/.env
```

---

## 第四步：重新构建前端

```bash
cd /opt/repair-system/frontend
npm install
npm run build
```

> ⏳ 构建约需 1 分钟

构建完成后部署到 Nginx 目录：

```bash
cp -r dist/* /var/www/repair-system/
chown -R www-data:www-data /var/www/repair-system/
```

---

## 第五步：重启后端

```bash
pm2 restart repair-api
```

等待约 5 秒后验证：

```bash
# 健康检查（注意：v1.5 起不再返回 version 字段）
curl http://127.0.0.1:8000/health
# 期望：{"status":"ok"}

# 登录接口是否正常（返回 422 = 接口存在、参数格式校验中）
curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/api/v1/auth/login
# 期望：422
```

---

## 第六步：重载 Nginx

```bash
systemctl reload nginx

# 验证无报错
nginx -t
# 期望：configuration file ... syntax is ok
```

---

## 第七步：功能验证

打开浏览器访问 `http://你的服务器IP`，用原有管理员账号登录：

- [ ] 登录正常，所有历史记录完整
- [ ] 维修记录、耗材管理、网络巡检、门禁巡检均可正常使用
- [ ] 连续输错密码超过10次后，登录接口返回 `429 Too Many Requests`（约1分钟后自动恢复）
- [ ] 批量导出 Word 传入非法日期（如 `../../etc`）返回 400 错误
- [ ] 后台新建用户时，密码少于8位或纯数字/纯字母会被拒绝

---

## 快速操作汇总

熟悉流程后，完整更新命令（服务器端）：

```bash
cd /opt/repair-system

# 1. 拉取代码
git pull

# 2. 安装新依赖
source backend/venv/bin/activate
pip install -r backend/requirements.txt
deactivate

# 3. 重建前端并部署
cd frontend && npm install && npm run build
cp -r dist/* /var/www/repair-system/ && chown -R www-data:www-data /var/www/repair-system/
cd ..

# 4. 重启后端
pm2 restart repair-api

# 5. 重载 Nginx
systemctl reload nginx

# 6. 验证
curl http://127.0.0.1:8000/health
```

---

## 回滚方案

如果更新后出现异常：

```bash
cd /opt/repair-system

# 查看提交历史，找到更新前的版本 hash
git log --oneline -5

# 回滚代码
git checkout 上一个版本的hash

# 重新构建前端
cd frontend && npm run build
cp -r dist/* /var/www/repair-system/
cd ..

# 重启后端
pm2 restart repair-api
systemctl reload nginx
```

> ✅ 回滚只影响代码，所有数据库记录（维修、巡检、用户账号）保留不变。

---

## 常见问题

### Q1：重启后 `pm2 list` 显示 `errored`

```bash
pm2 logs repair-api --lines 50
```

| 错误信息 | 解决方法 |
|----------|----------|
| `ModuleNotFoundError: No module named 'slowapi'` | 执行第二步，重新 `pip install -r requirements.txt` |
| `RuntimeError: 首次启动需设置环境变量 ADMIN_INIT_PASSWORD` | 数据库为空，执行第三步 3.2，设置 `ADMIN_INIT_PASSWORD` |
| `RuntimeError: 环境变量 SECRET_KEY 未设置` | `backend/.env` 中追加 `SECRET_KEY=随机密钥`（见 v1.4 教程第二步） |
| `Can't connect to MySQL` | `systemctl start mysql` |

### Q2：登录提示 429 Too Many Requests

同一 IP 在1分钟内尝试登录超过10次触发。等待1分钟后自动解锁，无需重启服务。

### Q3：新建用户/修改密码提示密码格式错误

v1.5 起密码要求升级：**至少8位，同时包含字母和数字**。例如：`Abc12345`、`repair2026` 均合法。纯数字、纯字母、6位密码将被拒绝。

存量用户的旧密码**不受影响**，照常可以登录，下次修改密码时才需要满足新规则。

### Q4：curl health 返回的 JSON 里没有 version 字段

正常现象。v1.5 起 `/health` 只返回 `{"status":"ok"}`，不再暴露版本号。

### Q5：上传照片失败（413 错误）

```bash
grep client_max_body_size /etc/nginx/sites-available/repair-system
```

若无该配置，在 `server {}` 块中加入：

```nginx
client_max_body_size 20M;
```

然后 `systemctl reload nginx`。

---

## 常用维护命令速查

| 操作 | 命令 |
|------|------|
| 查看后端状态 | `pm2 list` |
| 查看后端实时日志 | `pm2 logs repair-api` |
| 重启后端 | `pm2 restart repair-api` |
| 重载 Nginx（不中断） | `systemctl reload nginx` |
| 重启 MySQL | `systemctl restart mysql` |
| 查看 Nginx 错误日志 | `tail -50 /var/log/nginx/error.log` |
| 查看后端最近50行日志 | `pm2 logs repair-api --lines 50` |

---

*文档版本：v1.5（安全加固版） | 更新日期：2026-07-23*
