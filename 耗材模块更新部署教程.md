# 耗材管理模块 — 更新部署教程

> **适用场景**：已按《GITHUB部署教程.md》完成初次部署，项目正在运行中，现在需要更新上线「耗材管理」新模块。  
> **操作原则**：零停机拉取代码 → 仅在切换瞬间重启服务，现有数据库数据完全不受影响。  
> **服务器系统**：Ubuntu 20.04 / 22.04 LTS

---

## 更新内容说明

本次更新新增了以下内容：

| 类型 | 文件/功能 |
|------|----------|
| 后端模型 | `consumable_records`、`consumable_items`、`consumable_photos` 三张新表（**自动创建，无需手动建表**） |
| 后端接口 | `/api/v1/consumables` 完整 CRUD + 照片上传 + Word 导出 |
| 前端页面 | 「耗材管理」列表页、新建/编辑表单页、照片上传、Word 导出 |
| 导出格式 | 导出的 Word 严格匹配《云南农业职业技术学院电子设备网络安防耗材使用情况表》原始格式 |

> ✅ **Nginx 配置无需修改**，数据库无需手动执行任何 SQL，原有维修记录数据完全不受影响。

---

## 第一步：Windows 端推送新代码到 GitHub

在你的 **Windows 电脑**上，打开 CMD 命令提示符：

```bash
cd D:\维修记录web项目

# 查看变更文件（确认新增的耗材模块文件都在）
git status
```

应看到如下新增文件（如果没有，说明代码还未保存）：

```
backend/app/models/consumable.py
backend/app/schemas/consumable.py
backend/app/services/consumable_service.py
backend/app/services/consumable_word_export.py
backend/app/api/v1/consumable.py
frontend/src/api/consumable.js
frontend/src/views/ConsumableList.vue
frontend/src/views/ConsumableForm.vue
```

以及修改的文件：

```
backend/main.py
frontend/src/router/index.js
frontend/src/App.vue
frontend/dist/  （重新构建的前端产物）
```

确认无误后，推送到 GitHub：

```bash
# 添加所有改动
git add .

# 提交（说明本次更新内容）
git commit -m "feat: 新增耗材管理模块（CRUD + 照片上传 + Word导出）"

# 推送到 GitHub
git push
```

推送成功后，GitHub 仓库页面刷新，可看到新文件。

---

## 第二步：Linux 服务器端更新部署

登录你的服务器：

```bash
ssh root@你的服务器IP
```

### 2.1 拉取最新代码

```bash
cd /opt/repair-system

# 拉取代码（此步骤不影响正在运行的服务）
git pull

# 验证新文件已拉取
ls backend/app/models/consumable.py
ls backend/app/api/v1/consumable.py
ls frontend/src/views/ConsumableList.vue
# 以上每条命令应显示文件路径，不报错即为拉取成功
```

> ⚠️ 如果是私有仓库，拉取时需输入 GitHub 账号和 Token（同初次部署时相同的 Token）。

---

### 2.2 重新构建前端

前端源码（`frontend/src/`）已更新，需要在服务器上重新编译生成静态文件：

```bash
cd /opt/repair-system/frontend

# 安装依赖（如果 package.json 没有变化可跳过，但首次更新建议执行）
npm install

# 重新构建（约 30 秒）
npm run build

# 构建完成后验证新页面文件存在
ls dist/assets/ | grep -i consumable
# 应看到 ConsumableList 和 ConsumableForm 相关的 .js 文件

# 部署到 Nginx 目录
cp -r dist/* /var/www/repair-system/
chown -R www-data:www-data /var/www/repair-system/
```

> 💡 如果 npm 下载慢，先切换淘宝镜像再安装：
> ```bash
> npm config set registry https://registry.npmmirror.com
> npm install
> ```

---

### 2.3 重启后端服务（让新模块生效）

**重启后端时会有约 2-3 秒服务中断**，选择访问量少的时候操作：

```bash
pm2 restart repair-api
```

等待约 3 秒后，验证重启成功：

```bash
# 查看进程状态（应显示 online）
pm2 list

# 检查健康接口
curl http://127.0.0.1:8000/health
# 期望返回：{"status":"ok","version":"2.0.0"}

# 检查新接口是否响应（应返回 401，说明接口存在、认证生效）
curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/api/v1/consumables
# 期望返回：401
```

---

### 2.4 重载 Nginx（让前端更新生效）

此操作**不中断现有连接**：

```bash
systemctl reload nginx

# 验证 Nginx 无报错
nginx -t
# 期望输出：configuration file ... syntax is ok
#           configuration file ... test is successful
```

---

### 2.5 验证新表自动创建

后端启动时会自动在数据库中创建三张新表。验证：

```bash
# 登录 MySQL（输入你的数据库密码）
mysql -u root -p repair_db

# 在 MySQL 中执行
SHOW TABLES;
```

应看到表列表中新增了：

```
consumable_items
consumable_photos
consumable_records
```

以及原有的所有表均完好无损。退出 MySQL：

```sql
EXIT;
```

---

## 第三步：测试新功能

打开浏览器，访问 `http://你的服务器IP`，使用管理员账号登录。

**测试清单：**

- [ ] 顶部导航栏出现「耗材管理」菜单项
- [ ] 点击「耗材管理」→ 进入列表页，页面正常加载
- [ ] 点击「新建耗材记录」→ 填写地点、日期、耗材明细 → 保存成功
- [ ] 在编辑页上传一张现场照片 → 上传成功并显示缩略图
- [ ] 点击列表页「导出Word」→ 下载 `.docx` 文件 → 打开后格式与原始表格一致
- [ ] 原有「维修记录」功能正常使用（确认未受影响）

---

## 快速操作汇总

如果你对流程熟悉，以下是最精简的更新命令（服务器端）：

```bash
# 1. 拉取代码
cd /opt/repair-system && git pull

# 2. 更新前端文件
cp -r frontend/dist/* /var/www/repair-system/ && chown -R www-data:www-data /var/www/repair-system/

# 3. 重启后端（自动建表）
pm2 restart repair-api

# 4. 重载 Nginx
systemctl reload nginx

# 5. 验证
curl http://127.0.0.1:8000/health
```

---

## 回滚方案（如出现问题）

如果更新后出现异常，可立即回滚到上一版本：

```bash
cd /opt/repair-system

# 查看提交历史，找到更新前的版本 hash
git log --oneline -5

# 回滚到上一个版本（将 abc1234 替换为上一条提交的 hash）
git checkout abc1234

# 复制旧前端文件
cp -r frontend/dist/* /var/www/repair-system/
chown -R www-data:www-data /var/www/repair-system/

# 重启后端
pm2 restart repair-api

# 重载 Nginx
systemctl reload nginx
```

> ✅ 回滚只影响代码，三张新表即使回滚后仍存在于数据库中，不会造成任何损坏（只是不再被使用）。

---

## 常见问题

### Q1：`git pull` 后提示 "Already up to date"

说明代码没有推送成功，回到 Windows 端确认 `git push` 是否完成。

### Q2：重启后 `pm2 list` 显示 `errored`

查看详细错误：

```bash
pm2 logs repair-api --lines 50
```

常见原因及解决：

| 错误信息 | 解决方法 |
|----------|----------|
| `ModuleNotFoundError: No module named 'app.models.consumable'` | 确认 `git pull` 已拉取到新文件 |
| `cannot import name 'consumable' from 'app.api.v1'` | 同上，检查 `backend/app/api/v1/consumable.py` 是否存在 |
| `Can't connect to MySQL` | 执行 `systemctl start mysql` 重启数据库 |

### Q3：浏览器访问还是旧版（没有「耗材管理」菜单）

强制刷新浏览器缓存：按 `Ctrl + Shift + R`（Windows）或 `Cmd + Shift + R`（Mac）。

如果刷新后仍无变化：

```bash
# 确认前端文件已更新
ls -la /var/www/repair-system/assets/ | grep -i consumable
# 应看到 ConsumableList 和 ConsumableForm 的 .js 文件
```

### Q4：导出的 Word 文件打开乱码或图片不显示

```bash
# 确认上传目录权限正常
ls -la /opt/repair-system/backend/uploads/
# 应有 drwxr-xr-x 权限

# 如有问题，修复权限
chmod 755 /opt/repair-system/backend/uploads/
```

### Q5：新模块上传照片失败（413 错误）

Nginx 请求体大小限制，检查配置：

```bash
grep client_max_body_size /etc/nginx/sites-available/repair-system
# 应显示 20M 或以上
```

如果没有该行，编辑配置文件在 `location /api/` 块中添加，然后 `systemctl reload nginx`。

---

## 可选：服务器上重新构建前端

> 仅当服务器已安装 Node.js，且想确保前端代码是最新的时候执行。

```bash
cd /opt/repair-system/frontend

# 安装依赖（如果是首次构建）
npm install

# 构建
npm run build

# 部署到 Nginx
cp -r dist/* /var/www/repair-system/
chown -R www-data:www-data /var/www/repair-system/
```

---

## 常用维护命令速查

| 操作 | 命令 |
|------|------|
| 重启后端 | `pm2 restart repair-api` |
| 查看后端实时日志 | `pm2 logs repair-api` |
| 重载 Nginx（不中断） | `systemctl reload nginx` |
| 重启 MySQL | `systemctl restart mysql` |
| 查看所有进程状态 | `pm2 list` |
| 查看 Nginx 错误日志 | `tail -50 /var/log/nginx/error.log` |
| 查看后端最近50行日志 | `pm2 logs repair-api --lines 50` |

---

*文档版本：2.0（耗材模块更新版） | 更新日期：2026-07-20*
